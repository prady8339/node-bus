import React from 'react';
import {Stack} from "@mui/material";
import {PostCard} from "../components";

function Home(props) {
    return (
        <Stack>
            <PostCard/>
        </Stack>
    );
}

export default Home;